; generated by drom from package skeleton 'library'

(library
  (name slurp)
  (public_name slurp)
  (wrapped true)
  (libraries uri magic-mime lwt fpath fmt ezjsonm str lwt.unix)
  
  (preprocess (pps lwt_ppx))
  )


(documentation
  (package slurp))


